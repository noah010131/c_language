#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

int main(void)

{

char str[20];
 fgets(str,sizeof(str), stdi,sizeof(str), stdi,sizeof(str), stdin);
 puts(str);
char str1[20];

strcpy(str1, str);

printf("%s\n", str1);

return 0;

}
